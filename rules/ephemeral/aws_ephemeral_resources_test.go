// This file generated by `generator/main.go`. DO NOT EDIT

package ephemeral

import (
	"testing"

	"github.com/terraform-linters/tflint-plugin-sdk/helper"
)

func Test_AwsEphemeralResources(t *testing.T) {
	cases := []struct {
		Name     string
		Content  string
		Expected helper.Issues
		Fixed    string
	}{
		{
			Name: "basic aws_eks_cluster_auth",
			Content: `
data "aws_eks_cluster_auth" "test" {
}
`,
			Expected: helper.Issues{
				{
					Rule:    NewAwsEphemeralResourcesRule(),
					Message: `"aws_eks_cluster_auth" is a non-ephemeral data source, which means that all (sensitive) attributes are stored in state. Please use ephemeral resource "aws_eks_cluster_auth" instead.`,
				},
			},
			Fixed: `
ephemeral "aws_eks_cluster_auth" "test" {
}
`,
		},
		{
			Name: "basic aws_kms_secrets",
			Content: `
data "aws_kms_secrets" "test" {
}
`,
			Expected: helper.Issues{
				{
					Rule:    NewAwsEphemeralResourcesRule(),
					Message: `"aws_kms_secrets" is a non-ephemeral data source, which means that all (sensitive) attributes are stored in state. Please use ephemeral resource "aws_kms_secrets" instead.`,
				},
			},
			Fixed: `
ephemeral "aws_kms_secrets" "test" {
}
`,
		},
		{
			Name: "basic aws_lambda_invocation",
			Content: `
data "aws_lambda_invocation" "test" {
}
`,
			Expected: helper.Issues{
				{
					Rule:    NewAwsEphemeralResourcesRule(),
					Message: `"aws_lambda_invocation" is a non-ephemeral data source, which means that all (sensitive) attributes are stored in state. Please use ephemeral resource "aws_lambda_invocation" instead.`,
				},
			},
			Fixed: `
ephemeral "aws_lambda_invocation" "test" {
}
`,
		},
		{
			Name: "basic aws_secretsmanager_random_password",
			Content: `
data "aws_secretsmanager_random_password" "test" {
}
`,
			Expected: helper.Issues{
				{
					Rule:    NewAwsEphemeralResourcesRule(),
					Message: `"aws_secretsmanager_random_password" is a non-ephemeral data source, which means that all (sensitive) attributes are stored in state. Please use ephemeral resource "aws_secretsmanager_random_password" instead.`,
				},
			},
			Fixed: `
ephemeral "aws_secretsmanager_random_password" "test" {
}
`,
		},
		{
			Name: "basic aws_secretsmanager_secret_version",
			Content: `
data "aws_secretsmanager_secret_version" "test" {
}
`,
			Expected: helper.Issues{
				{
					Rule:    NewAwsEphemeralResourcesRule(),
					Message: `"aws_secretsmanager_secret_version" is a non-ephemeral data source, which means that all (sensitive) attributes are stored in state. Please use ephemeral resource "aws_secretsmanager_secret_version" instead.`,
				},
			},
			Fixed: `
ephemeral "aws_secretsmanager_secret_version" "test" {
}
`,
		},
		{
			Name: "basic aws_ssm_parameter",
			Content: `
data "aws_ssm_parameter" "test" {
}
`,
			Expected: helper.Issues{
				{
					Rule:    NewAwsEphemeralResourcesRule(),
					Message: `"aws_ssm_parameter" is a non-ephemeral data source, which means that all (sensitive) attributes are stored in state. Please use ephemeral resource "aws_ssm_parameter" instead.`,
				},
			},
			Fixed: `
ephemeral "aws_ssm_parameter" "test" {
}
`,
		},
	}

	rule := NewAwsEphemeralResourcesRule()

	for _, tc := range cases {
		filename := "resource.tf"
		runner := helper.TestRunner(t, map[string]string{filename: tc.Content})

		if err := rule.Check(runner); err != nil {
			t.Fatalf("Unexpected error occurred: %s", err)
		}
		helper.AssertIssuesWithoutRange(t, tc.Expected, runner.Issues)

		want := map[string]string{}
		if tc.Fixed != "" {
			want[filename] = tc.Fixed
		}
		helper.AssertChanges(t, want, runner.Changes())
	}
}
