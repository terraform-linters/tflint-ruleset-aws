// This file generated by `generator/`. DO NOT EDIT

package models

import (
	"fmt"
	"regexp"

	"github.com/terraform-linters/tflint-plugin-sdk/hclext"
	"github.com/terraform-linters/tflint-plugin-sdk/logger"
	"github.com/terraform-linters/tflint-plugin-sdk/tflint"
)

// AwsSecretsmanagerSecretRotationInvalidRotationRulesRule checks the pattern is valid
type AwsSecretsmanagerSecretRotationInvalidRotationRulesRule struct {
	tflint.DefaultRule

	resourceType  string
	attributeName string
	keyMax        int
	keyMin        int
	keyPattern    *regexp.Regexp
	valueMax      int
	valueMin      int
	valuePattern  *regexp.Regexp
}

// NewAwsSecretsmanagerSecretRotationInvalidRotationRulesRule returns new rule with default attributes
func NewAwsSecretsmanagerSecretRotationInvalidRotationRulesRule() *AwsSecretsmanagerSecretRotationInvalidRotationRulesRule {
	return &AwsSecretsmanagerSecretRotationInvalidRotationRulesRule{
		resourceType:  "aws_secretsmanager_secret_rotation",
		attributeName: "rotation_rules",
		keyMax:        3,
		keyMin:        2,
		keyPattern:    regexp.MustCompile(`^[0-9]+h$`),
		valueMax:      256,
		valueMin:      1,
		valuePattern:  regexp.MustCompile(`^[0-9A-Za-z\(\)#\?\*\-\/, ]+$`),
	}
}

// Name returns the rule name
func (r *AwsSecretsmanagerSecretRotationInvalidRotationRulesRule) Name() string {
	return "aws_secretsmanager_secret_rotation_invalid_rotation_rules"
}

// Enabled returns whether the rule is enabled by default
func (r *AwsSecretsmanagerSecretRotationInvalidRotationRulesRule) Enabled() bool {
	return true
}

// Severity returns the rule severity
func (r *AwsSecretsmanagerSecretRotationInvalidRotationRulesRule) Severity() tflint.Severity {
	return tflint.ERROR
}

// Link returns the rule reference link
func (r *AwsSecretsmanagerSecretRotationInvalidRotationRulesRule) Link() string {
	return ""
}

// Check checks the pattern is valid
func (r *AwsSecretsmanagerSecretRotationInvalidRotationRulesRule) Check(runner tflint.Runner) error {
	logger.Trace("Check `%s` rule", r.Name())

	resources, err := runner.GetResourceContent(r.resourceType, &hclext.BodySchema{
		Attributes: []hclext.AttributeSchema{
			{Name: r.attributeName},
		},
	}, nil)
	if err != nil {
		return err
	}

	for _, resource := range resources.Blocks {
		attribute, exists := resource.Body.Attributes[r.attributeName]
		if !exists {
			continue
		}

		err := runner.EvaluateExpr(attribute.Expr, func(val map[string]string) error {
			for k, v := range val {
				if len(k) > r.keyMax {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag key %q must be 3 characters or less", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if len(k) < r.keyMin {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag key %q must be 2 characters or higher", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if !r.keyPattern.MatchString(k) {
					runner.EmitIssue(
						r,
						fmt.Sprintf(`tag key %q does not match valid pattern %s`, truncateLongMessage(k), `^[0-9]+h$`),
						attribute.Expr.Range(),
					)
				}
				if len(v) > r.valueMax {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag value for key %q must be 256 characters or less", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if len(v) < r.valueMin {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag value for key %q must be 1 characters or higher", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if !r.valuePattern.MatchString(v) {
					runner.EmitIssue(
						r,
						fmt.Sprintf(`tag value %q for key %q does not match valid pattern %s`, truncateLongMessage(v), truncateLongMessage(k), `^[0-9A-Za-z\(\)#\?\*\-\/, ]+$`),
						attribute.Expr.Range(),
					)
				}
			}
			return nil
		}, nil)
		if err != nil {
			return err
		}
	}

	return nil
}
