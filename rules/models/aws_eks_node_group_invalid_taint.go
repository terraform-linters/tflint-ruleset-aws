// This file generated by `generator/`. DO NOT EDIT

package models

import (
	"fmt"

	"github.com/terraform-linters/tflint-plugin-sdk/hclext"
	"github.com/terraform-linters/tflint-plugin-sdk/logger"
	"github.com/terraform-linters/tflint-plugin-sdk/tflint"
)

// AwsEksNodeGroupInvalidTaintRule checks the pattern is valid
type AwsEksNodeGroupInvalidTaintRule struct {
	tflint.DefaultRule

	resourceType  string
	attributeName string
	keyMax        int
	keyMin        int
	valueMax      int
}

// NewAwsEksNodeGroupInvalidTaintRule returns new rule with default attributes
func NewAwsEksNodeGroupInvalidTaintRule() *AwsEksNodeGroupInvalidTaintRule {
	return &AwsEksNodeGroupInvalidTaintRule{
		resourceType:  "aws_eks_node_group",
		attributeName: "taint",
		keyMax:        63,
		keyMin:        1,
		valueMax:      63,
	}
}

// Name returns the rule name
func (r *AwsEksNodeGroupInvalidTaintRule) Name() string {
	return "aws_eks_node_group_invalid_taint"
}

// Enabled returns whether the rule is enabled by default
func (r *AwsEksNodeGroupInvalidTaintRule) Enabled() bool {
	return true
}

// Severity returns the rule severity
func (r *AwsEksNodeGroupInvalidTaintRule) Severity() tflint.Severity {
	return tflint.ERROR
}

// Link returns the rule reference link
func (r *AwsEksNodeGroupInvalidTaintRule) Link() string {
	return ""
}

// Check checks the pattern is valid
func (r *AwsEksNodeGroupInvalidTaintRule) Check(runner tflint.Runner) error {
	logger.Trace("Check `%s` rule", r.Name())

	resources, err := runner.GetResourceContent(r.resourceType, &hclext.BodySchema{
		Attributes: []hclext.AttributeSchema{
			{Name: r.attributeName},
		},
	}, nil)
	if err != nil {
		return err
	}

	for _, resource := range resources.Blocks {
		attribute, exists := resource.Body.Attributes[r.attributeName]
		if !exists {
			continue
		}

		err := runner.EvaluateExpr(attribute.Expr, func(val map[string]string) error {
			for k, v := range val {
				if len(k) > r.keyMax {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag key %q must be 63 characters or less", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if len(k) < r.keyMin {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag key %q must be 1 characters or higher", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if len(v) > r.valueMax {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag value for key %q must be 63 characters or less", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
			}
			return nil
		}, nil)
		if err != nil {
			return err
		}
	}

	return nil
}
