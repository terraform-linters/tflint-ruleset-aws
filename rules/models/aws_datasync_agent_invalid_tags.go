// Code generated by rules/models/generator; DO NOT EDIT.

package models

import (
	"fmt"
	"regexp"

	"github.com/terraform-linters/tflint-plugin-sdk/hclext"
	"github.com/terraform-linters/tflint-plugin-sdk/logger"
	"github.com/terraform-linters/tflint-plugin-sdk/tflint"
)

// AwsDatasyncAgentInvalidTagsRule validates map keys and values
type AwsDatasyncAgentInvalidTagsRule struct {
	tflint.DefaultRule

	resourceType  string
	attributeName string
	itemsMax      int
	keyMax        int
	keyMin        int
	keyPattern    *regexp.Regexp
	valueMax      int
	valuePattern  *regexp.Regexp
}

// NewAwsDatasyncAgentInvalidTagsRule returns new rule with default attributes
func NewAwsDatasyncAgentInvalidTagsRule() *AwsDatasyncAgentInvalidTagsRule {
	return &AwsDatasyncAgentInvalidTagsRule{
		resourceType:  "aws_datasync_agent",
		attributeName: "tags",
		itemsMax:      50,
		keyMax:        256,
		keyMin:        1,
		keyPattern:    regexp.MustCompile(`^[a-zA-Z0-9\s+=._:/-]+$`),
		valueMax:      256,
		valuePattern:  regexp.MustCompile(`^[a-zA-Z0-9\s+=._:@/-]+$`),
	}
}

// Name returns the rule name
func (r *AwsDatasyncAgentInvalidTagsRule) Name() string {
	return "aws_datasync_agent_invalid_tags"
}

// Enabled returns whether the rule is enabled by default
func (r *AwsDatasyncAgentInvalidTagsRule) Enabled() bool {
	return true
}

// Severity returns the rule severity
func (r *AwsDatasyncAgentInvalidTagsRule) Severity() tflint.Severity {
	return tflint.ERROR
}

// Link returns the rule reference link
func (r *AwsDatasyncAgentInvalidTagsRule) Link() string {
	return ""
}

// Check validates map keys and values
func (r *AwsDatasyncAgentInvalidTagsRule) Check(runner tflint.Runner) error {
	logger.Trace("Check `%s` rule", r.Name())

	resources, err := runner.GetResourceContent(r.resourceType, &hclext.BodySchema{
		Attributes: []hclext.AttributeSchema{
			{Name: r.attributeName},
		},
	}, nil)
	if err != nil {
		return err
	}

	for _, resource := range resources.Blocks {
		attribute, exists := resource.Body.Attributes[r.attributeName]
		if !exists {
			continue
		}

		err := runner.EvaluateExpr(attribute.Expr, func(val map[string]string) error {
			if len(val) > r.itemsMax {
				runner.EmitIssue(
					r,
					fmt.Sprintf("too many tags: %d exceeds the maximum of 50", len(val)),
					attribute.Expr.Range(),
				)
			}
			for k, v := range val {
				if len(k) > r.keyMax {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tags key %q must be 256 characters or less", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if len(k) < r.keyMin {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tags key %q must be at least 1 character", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if !r.keyPattern.MatchString(k) {
					runner.EmitIssue(
						r,
						fmt.Sprintf(`tags key %q does not match valid pattern %s`, truncateLongMessage(k), `^[a-zA-Z0-9\s+=._:/-]+$`),
						attribute.Expr.Range(),
					)
				}
				if len(v) > r.valueMax {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tags value for key %q must be 256 characters or less", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if !r.valuePattern.MatchString(v) {
					runner.EmitIssue(
						r,
						fmt.Sprintf(`tags value %q for key %q does not match valid pattern %s`, truncateLongMessage(v), truncateLongMessage(k), `^[a-zA-Z0-9\s+=._:@/-]+$`),
						attribute.Expr.Range(),
					)
				}
			}
			return nil
		}, nil)
		if err != nil {
			return err
		}
	}

	return nil
}
