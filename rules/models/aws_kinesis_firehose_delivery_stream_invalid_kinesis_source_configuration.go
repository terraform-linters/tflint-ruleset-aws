// This file generated by `generator/`. DO NOT EDIT

package models

import (
	"fmt"
	"regexp"

	"github.com/terraform-linters/tflint-plugin-sdk/hclext"
	"github.com/terraform-linters/tflint-plugin-sdk/logger"
	"github.com/terraform-linters/tflint-plugin-sdk/tflint"
)

// AwsKinesisFirehoseDeliveryStreamInvalidKinesisSourceConfigurationRule checks the pattern is valid
type AwsKinesisFirehoseDeliveryStreamInvalidKinesisSourceConfigurationRule struct {
	tflint.DefaultRule

	resourceType  string
	attributeName string
	keyMax        int
	keyMin        int
	keyPattern    *regexp.Regexp
	valueMax      int
	valueMin      int
	valuePattern  *regexp.Regexp
}

// NewAwsKinesisFirehoseDeliveryStreamInvalidKinesisSourceConfigurationRule returns new rule with default attributes
func NewAwsKinesisFirehoseDeliveryStreamInvalidKinesisSourceConfigurationRule() *AwsKinesisFirehoseDeliveryStreamInvalidKinesisSourceConfigurationRule {
	return &AwsKinesisFirehoseDeliveryStreamInvalidKinesisSourceConfigurationRule{
		resourceType:  "aws_kinesis_firehose_delivery_stream",
		attributeName: "kinesis_source_configuration",
		keyMax:        512,
		keyMin:        1,
		keyPattern:    regexp.MustCompile(`^arn:.*:kinesis:[a-zA-Z0-9\-]+:\d{12}:stream/[a-zA-Z0-9_.-]+$`),
		valueMax:      512,
		valueMin:      1,
		valuePattern:  regexp.MustCompile(`^arn:.*:iam::\d{12}:role/[a-zA-Z_0-9+=,.@\-_/]+$`),
	}
}

// Name returns the rule name
func (r *AwsKinesisFirehoseDeliveryStreamInvalidKinesisSourceConfigurationRule) Name() string {
	return "aws_kinesis_firehose_delivery_stream_invalid_kinesis_source_configuration"
}

// Enabled returns whether the rule is enabled by default
func (r *AwsKinesisFirehoseDeliveryStreamInvalidKinesisSourceConfigurationRule) Enabled() bool {
	return true
}

// Severity returns the rule severity
func (r *AwsKinesisFirehoseDeliveryStreamInvalidKinesisSourceConfigurationRule) Severity() tflint.Severity {
	return tflint.ERROR
}

// Link returns the rule reference link
func (r *AwsKinesisFirehoseDeliveryStreamInvalidKinesisSourceConfigurationRule) Link() string {
	return ""
}

// Check checks the pattern is valid
func (r *AwsKinesisFirehoseDeliveryStreamInvalidKinesisSourceConfigurationRule) Check(runner tflint.Runner) error {
	logger.Trace("Check `%s` rule", r.Name())

	resources, err := runner.GetResourceContent(r.resourceType, &hclext.BodySchema{
		Attributes: []hclext.AttributeSchema{
			{Name: r.attributeName},
		},
	}, nil)
	if err != nil {
		return err
	}

	for _, resource := range resources.Blocks {
		attribute, exists := resource.Body.Attributes[r.attributeName]
		if !exists {
			continue
		}

		err := runner.EvaluateExpr(attribute.Expr, func(val map[string]string) error {
			for k, v := range val {
				if len(k) > r.keyMax {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag key %q must be 512 characters or less", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if len(k) < r.keyMin {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag key %q must be 1 characters or higher", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if !r.keyPattern.MatchString(k) {
					runner.EmitIssue(
						r,
						fmt.Sprintf(`tag key %q does not match valid pattern %s`, truncateLongMessage(k), `^arn:.*:kinesis:[a-zA-Z0-9\-]+:\d{12}:stream/[a-zA-Z0-9_.-]+$`),
						attribute.Expr.Range(),
					)
				}
				if len(v) > r.valueMax {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag value for key %q must be 512 characters or less", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if len(v) < r.valueMin {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag value for key %q must be 1 characters or higher", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if !r.valuePattern.MatchString(v) {
					runner.EmitIssue(
						r,
						fmt.Sprintf(`tag value %q for key %q does not match valid pattern %s`, truncateLongMessage(v), truncateLongMessage(k), `^arn:.*:iam::\d{12}:role/[a-zA-Z_0-9+=,.@\-_/]+$`),
						attribute.Expr.Range(),
					)
				}
			}
			return nil
		}, nil)
		if err != nil {
			return err
		}
	}

	return nil
}
