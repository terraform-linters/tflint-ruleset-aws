// This file generated by `generator/`. DO NOT EDIT

package models

import (
	"fmt"
	"regexp"

	"github.com/terraform-linters/tflint-plugin-sdk/hclext"
	"github.com/terraform-linters/tflint-plugin-sdk/logger"
	"github.com/terraform-linters/tflint-plugin-sdk/tflint"
)

// AwsSagemakerWorkforceInvalidCognitoConfigRule checks the pattern is valid
type AwsSagemakerWorkforceInvalidCognitoConfigRule struct {
	tflint.DefaultRule

	resourceType  string
	attributeName string
	keyMax        int
	keyMin        int
	keyPattern    *regexp.Regexp
	valueMax      int
	valueMin      int
	valuePattern  *regexp.Regexp
}

// NewAwsSagemakerWorkforceInvalidCognitoConfigRule returns new rule with default attributes
func NewAwsSagemakerWorkforceInvalidCognitoConfigRule() *AwsSagemakerWorkforceInvalidCognitoConfigRule {
	return &AwsSagemakerWorkforceInvalidCognitoConfigRule{
		resourceType:  "aws_sagemaker_workforce",
		attributeName: "cognito_config",
		keyMax:        1024,
		keyMin:        1,
		keyPattern:    regexp.MustCompile(`^[ -~]+$`),
		valueMax:      55,
		valueMin:      1,
		valuePattern:  regexp.MustCompile(`^[\w-]+_[0-9a-zA-Z]+$`),
	}
}

// Name returns the rule name
func (r *AwsSagemakerWorkforceInvalidCognitoConfigRule) Name() string {
	return "aws_sagemaker_workforce_invalid_cognito_config"
}

// Enabled returns whether the rule is enabled by default
func (r *AwsSagemakerWorkforceInvalidCognitoConfigRule) Enabled() bool {
	return true
}

// Severity returns the rule severity
func (r *AwsSagemakerWorkforceInvalidCognitoConfigRule) Severity() tflint.Severity {
	return tflint.ERROR
}

// Link returns the rule reference link
func (r *AwsSagemakerWorkforceInvalidCognitoConfigRule) Link() string {
	return ""
}

// Check checks the pattern is valid
func (r *AwsSagemakerWorkforceInvalidCognitoConfigRule) Check(runner tflint.Runner) error {
	logger.Trace("Check `%s` rule", r.Name())

	resources, err := runner.GetResourceContent(r.resourceType, &hclext.BodySchema{
		Attributes: []hclext.AttributeSchema{
			{Name: r.attributeName},
		},
	}, nil)
	if err != nil {
		return err
	}

	for _, resource := range resources.Blocks {
		attribute, exists := resource.Body.Attributes[r.attributeName]
		if !exists {
			continue
		}

		err := runner.EvaluateExpr(attribute.Expr, func(val map[string]string) error {
			for k, v := range val {
				if len(k) > r.keyMax {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag key %q must be 1024 characters or less", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if len(k) < r.keyMin {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag key %q must be 1 characters or higher", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if !r.keyPattern.MatchString(k) {
					runner.EmitIssue(
						r,
						fmt.Sprintf(`tag key %q does not match valid pattern %s`, truncateLongMessage(k), `^[ -~]+$`),
						attribute.Expr.Range(),
					)
				}
				if len(v) > r.valueMax {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag value for key %q must be 55 characters or less", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if len(v) < r.valueMin {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag value for key %q must be 1 characters or higher", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if !r.valuePattern.MatchString(v) {
					runner.EmitIssue(
						r,
						fmt.Sprintf(`tag value %q for key %q does not match valid pattern %s`, truncateLongMessage(v), truncateLongMessage(k), `^[\w-]+_[0-9a-zA-Z]+$`),
						attribute.Expr.Range(),
					)
				}
			}
			return nil
		}, nil)
		if err != nil {
			return err
		}
	}

	return nil
}
