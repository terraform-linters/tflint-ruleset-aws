// This file generated by `generator/`. DO NOT EDIT

package models

import (
	"fmt"
	"regexp"

	"github.com/terraform-linters/tflint-plugin-sdk/hclext"
	"github.com/terraform-linters/tflint-plugin-sdk/logger"
	"github.com/terraform-linters/tflint-plugin-sdk/tflint"
)

// AwsTransferServerInvalidEndpointDetailsRule checks the pattern is valid
type AwsTransferServerInvalidEndpointDetailsRule struct {
	tflint.DefaultRule

	resourceType  string
	attributeName string
	keyMax        int
	keyMin        int
	keyPattern    *regexp.Regexp
}

// NewAwsTransferServerInvalidEndpointDetailsRule returns new rule with default attributes
func NewAwsTransferServerInvalidEndpointDetailsRule() *AwsTransferServerInvalidEndpointDetailsRule {
	return &AwsTransferServerInvalidEndpointDetailsRule{
		resourceType:  "aws_transfer_server",
		attributeName: "endpoint_details",
		keyMax:        22,
		keyMin:        22,
		keyPattern:    regexp.MustCompile(`^vpce-[0-9a-f]{17}$`),
	}
}

// Name returns the rule name
func (r *AwsTransferServerInvalidEndpointDetailsRule) Name() string {
	return "aws_transfer_server_invalid_endpoint_details"
}

// Enabled returns whether the rule is enabled by default
func (r *AwsTransferServerInvalidEndpointDetailsRule) Enabled() bool {
	return true
}

// Severity returns the rule severity
func (r *AwsTransferServerInvalidEndpointDetailsRule) Severity() tflint.Severity {
	return tflint.ERROR
}

// Link returns the rule reference link
func (r *AwsTransferServerInvalidEndpointDetailsRule) Link() string {
	return ""
}

// Check checks the pattern is valid
func (r *AwsTransferServerInvalidEndpointDetailsRule) Check(runner tflint.Runner) error {
	logger.Trace("Check `%s` rule", r.Name())

	resources, err := runner.GetResourceContent(r.resourceType, &hclext.BodySchema{
		Attributes: []hclext.AttributeSchema{
			{Name: r.attributeName},
		},
	}, nil)
	if err != nil {
		return err
	}

	for _, resource := range resources.Blocks {
		attribute, exists := resource.Body.Attributes[r.attributeName]
		if !exists {
			continue
		}

		err := runner.EvaluateExpr(attribute.Expr, func(val map[string]string) error {
			for k, _ := range val {
				if len(k) > r.keyMax {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag key %q must be 22 characters or less", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if len(k) < r.keyMin {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag key %q must be 22 characters or higher", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if !r.keyPattern.MatchString(k) {
					runner.EmitIssue(
						r,
						fmt.Sprintf(`tag key %q does not match valid pattern %s`, truncateLongMessage(k), `^vpce-[0-9a-f]{17}$`),
						attribute.Expr.Range(),
					)
				}
			}
			return nil
		}, nil)
		if err != nil {
			return err
		}
	}

	return nil
}
