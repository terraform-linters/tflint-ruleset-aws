// Code generated by rules/models/generator; DO NOT EDIT.

package models

import (
{{- if .NeedsRegexp }}
	"regexp"
{{ end }}
	"github.com/terraform-linters/tflint-plugin-sdk/tflint"
)

// mapRules validate map attributes against constraints from AWS API models.
var mapRules = []tflint.Rule{
{{- range .Rules }}
	&mapRule{
		name:          "{{ .RuleName }}",
		resourceType:  "{{ .ResourceType }}",
		attributeName: "{{ .AttributeName }}",
{{- if .Sensitive }}
		sensitive: true,
{{- end }}
{{- if ne .ItemsMax 0 }}
		itemsMax: {{ .ItemsMax }},
{{- end }}
{{- if ne .KeyMax 0 }}
		keyMax: {{ .KeyMax }},
{{- end }}
{{- if ne .KeyMin 0 }}
		keyMin: {{ .KeyMin }},
{{- end }}
{{- if ne .KeyPattern "" }}
		keyPattern: regexp.MustCompile(`{{ .KeyPattern }}`),
{{- end }}
{{- if .KeyPrefixDeny }}
		keyPrefixDeny: []string{ {{- range $i, $p := .KeyPrefixDeny }}{{ if $i }}, {{ end }}"{{ $p }}"{{ end -}} },
{{- end }}
{{- if ne .ValueMax 0 }}
		valueMax: {{ .ValueMax }},
{{- end }}
{{- if ne .ValueMin 0 }}
		valueMin: {{ .ValueMin }},
{{- end }}
{{- if ne .ValuePattern "" }}
		valuePattern: regexp.MustCompile(`{{ .ValuePattern }}`),
{{- end }}
{{- if .ValuePrefixDeny }}
		valuePrefixDeny: []string{ {{- range $i, $p := .ValuePrefixDeny }}{{ if $i }}, {{ end }}"{{ $p }}"{{ end -}} },
{{- end }}
	},
{{- end }}
}
