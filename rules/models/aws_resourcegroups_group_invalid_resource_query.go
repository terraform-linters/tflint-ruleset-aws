// This file generated by `generator/`. DO NOT EDIT

package models

import (
	"fmt"
	"regexp"

	"github.com/terraform-linters/tflint-plugin-sdk/hclext"
	"github.com/terraform-linters/tflint-plugin-sdk/logger"
	"github.com/terraform-linters/tflint-plugin-sdk/tflint"
)

// AwsResourcegroupsGroupInvalidResourceQueryRule checks the pattern is valid
type AwsResourcegroupsGroupInvalidResourceQueryRule struct {
	tflint.DefaultRule

	resourceType  string
	attributeName string
	keyMax        int
	keyPattern    *regexp.Regexp
	valueMax      int
	valueMin      int
	valuePattern  *regexp.Regexp
}

// NewAwsResourcegroupsGroupInvalidResourceQueryRule returns new rule with default attributes
func NewAwsResourcegroupsGroupInvalidResourceQueryRule() *AwsResourcegroupsGroupInvalidResourceQueryRule {
	return &AwsResourcegroupsGroupInvalidResourceQueryRule{
		resourceType:  "aws_resourcegroups_group",
		attributeName: "resource_query",
		keyMax:        4096,
		keyPattern:    regexp.MustCompile(`^[\s\S]*$`),
		valueMax:      128,
		valueMin:      1,
		valuePattern:  regexp.MustCompile(`^\w+$`),
	}
}

// Name returns the rule name
func (r *AwsResourcegroupsGroupInvalidResourceQueryRule) Name() string {
	return "aws_resourcegroups_group_invalid_resource_query"
}

// Enabled returns whether the rule is enabled by default
func (r *AwsResourcegroupsGroupInvalidResourceQueryRule) Enabled() bool {
	return true
}

// Severity returns the rule severity
func (r *AwsResourcegroupsGroupInvalidResourceQueryRule) Severity() tflint.Severity {
	return tflint.ERROR
}

// Link returns the rule reference link
func (r *AwsResourcegroupsGroupInvalidResourceQueryRule) Link() string {
	return ""
}

// Check checks the pattern is valid
func (r *AwsResourcegroupsGroupInvalidResourceQueryRule) Check(runner tflint.Runner) error {
	logger.Trace("Check `%s` rule", r.Name())

	resources, err := runner.GetResourceContent(r.resourceType, &hclext.BodySchema{
		Attributes: []hclext.AttributeSchema{
			{Name: r.attributeName},
		},
	}, nil)
	if err != nil {
		return err
	}

	for _, resource := range resources.Blocks {
		attribute, exists := resource.Body.Attributes[r.attributeName]
		if !exists {
			continue
		}

		err := runner.EvaluateExpr(attribute.Expr, func(val map[string]string) error {
			for k, v := range val {
				if len(k) > r.keyMax {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag key %q must be 4096 characters or less", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if !r.keyPattern.MatchString(k) {
					runner.EmitIssue(
						r,
						fmt.Sprintf(`tag key %q does not match valid pattern %s`, truncateLongMessage(k), `^[\s\S]*$`),
						attribute.Expr.Range(),
					)
				}
				if len(v) > r.valueMax {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag value for key %q must be 128 characters or less", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if len(v) < r.valueMin {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag value for key %q must be 1 characters or higher", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if !r.valuePattern.MatchString(v) {
					runner.EmitIssue(
						r,
						fmt.Sprintf(`tag value %q for key %q does not match valid pattern %s`, truncateLongMessage(v), truncateLongMessage(k), `^\w+$`),
						attribute.Expr.Range(),
					)
				}
			}
			return nil
		}, nil)
		if err != nil {
			return err
		}
	}

	return nil
}
