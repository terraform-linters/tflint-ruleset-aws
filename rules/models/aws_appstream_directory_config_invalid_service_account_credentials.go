// This file generated by `generator/`. DO NOT EDIT

package models

import (
	"fmt"

	"github.com/terraform-linters/tflint-plugin-sdk/hclext"
	"github.com/terraform-linters/tflint-plugin-sdk/logger"
	"github.com/terraform-linters/tflint-plugin-sdk/tflint"
)

// AwsAppstreamDirectoryConfigInvalidServiceAccountCredentialsRule checks the pattern is valid
type AwsAppstreamDirectoryConfigInvalidServiceAccountCredentialsRule struct {
	tflint.DefaultRule

	resourceType  string
	attributeName string
	keyMin        int
	valueMax      int
	valueMin      int
}

// NewAwsAppstreamDirectoryConfigInvalidServiceAccountCredentialsRule returns new rule with default attributes
func NewAwsAppstreamDirectoryConfigInvalidServiceAccountCredentialsRule() *AwsAppstreamDirectoryConfigInvalidServiceAccountCredentialsRule {
	return &AwsAppstreamDirectoryConfigInvalidServiceAccountCredentialsRule{
		resourceType:  "aws_appstream_directory_config",
		attributeName: "service_account_credentials",
		keyMin:        1,
		valueMax:      127,
		valueMin:      1,
	}
}

// Name returns the rule name
func (r *AwsAppstreamDirectoryConfigInvalidServiceAccountCredentialsRule) Name() string {
	return "aws_appstream_directory_config_invalid_service_account_credentials"
}

// Enabled returns whether the rule is enabled by default
func (r *AwsAppstreamDirectoryConfigInvalidServiceAccountCredentialsRule) Enabled() bool {
	return true
}

// Severity returns the rule severity
func (r *AwsAppstreamDirectoryConfigInvalidServiceAccountCredentialsRule) Severity() tflint.Severity {
	return tflint.ERROR
}

// Link returns the rule reference link
func (r *AwsAppstreamDirectoryConfigInvalidServiceAccountCredentialsRule) Link() string {
	return ""
}

// Check checks the pattern is valid
func (r *AwsAppstreamDirectoryConfigInvalidServiceAccountCredentialsRule) Check(runner tflint.Runner) error {
	logger.Trace("Check `%s` rule", r.Name())

	resources, err := runner.GetResourceContent(r.resourceType, &hclext.BodySchema{
		Attributes: []hclext.AttributeSchema{
			{Name: r.attributeName},
		},
	}, nil)
	if err != nil {
		return err
	}

	for _, resource := range resources.Blocks {
		attribute, exists := resource.Body.Attributes[r.attributeName]
		if !exists {
			continue
		}

		err := runner.EvaluateExpr(attribute.Expr, func(val map[string]string) error {
			for k, v := range val {
				if len(k) < r.keyMin {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag key %q must be 1 characters or higher", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if len(v) > r.valueMax {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag value for key %q must be 127 characters or less", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if len(v) < r.valueMin {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag value for key %q must be 1 characters or higher", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
			}
			return nil
		}, nil)
		if err != nil {
			return err
		}
	}

	return nil
}
