// Code generated by rules/models/generator; DO NOT EDIT.

package models

import (
	"fmt"
	"regexp"
	"strings"

	"github.com/terraform-linters/tflint-plugin-sdk/hclext"
	"github.com/terraform-linters/tflint-plugin-sdk/logger"
	"github.com/terraform-linters/tflint-plugin-sdk/tflint"
)

// AwsConnectHoursOfOperationInvalidTagsRule validates map keys and values
type AwsConnectHoursOfOperationInvalidTagsRule struct {
	tflint.DefaultRule

	resourceType  string
	attributeName string
	keyMax        int
	keyMin        int
	keyPattern    *regexp.Regexp
	valueMax      int
}

// NewAwsConnectHoursOfOperationInvalidTagsRule returns new rule with default attributes
func NewAwsConnectHoursOfOperationInvalidTagsRule() *AwsConnectHoursOfOperationInvalidTagsRule {
	return &AwsConnectHoursOfOperationInvalidTagsRule{
		resourceType:  "aws_connect_hours_of_operation",
		attributeName: "tags",
		keyMax:        128,
		keyMin:        1,
		keyPattern:    regexp.MustCompile(`^[\p{L}\p{Z}\p{N}_.:/=+\-@]*$`),
		valueMax:      256,
	}
}

// Name returns the rule name
func (r *AwsConnectHoursOfOperationInvalidTagsRule) Name() string {
	return "aws_connect_hours_of_operation_invalid_tags"
}

// Enabled returns whether the rule is enabled by default
func (r *AwsConnectHoursOfOperationInvalidTagsRule) Enabled() bool {
	return true
}

// Severity returns the rule severity
func (r *AwsConnectHoursOfOperationInvalidTagsRule) Severity() tflint.Severity {
	return tflint.ERROR
}

// Link returns the rule reference link
func (r *AwsConnectHoursOfOperationInvalidTagsRule) Link() string {
	return ""
}

// Check validates map keys and values
func (r *AwsConnectHoursOfOperationInvalidTagsRule) Check(runner tflint.Runner) error {
	logger.Trace("Check `%s` rule", r.Name())

	resources, err := runner.GetResourceContent(r.resourceType, &hclext.BodySchema{
		Attributes: []hclext.AttributeSchema{
			{Name: r.attributeName},
		},
	}, nil)
	if err != nil {
		return err
	}

	for _, resource := range resources.Blocks {
		attribute, exists := resource.Body.Attributes[r.attributeName]
		if !exists {
			continue
		}

		err := runner.EvaluateExpr(attribute.Expr, func(val map[string]string) error {
			for k, v := range val {
				if len(k) > r.keyMax {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tags key %q must be 128 characters or less", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if len(k) < r.keyMin {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tags key %q must be at least 1 character", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if strings.HasPrefix(k, "aws:") {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tags key %q must not start with %q", truncateLongMessage(k), "aws:"),
						attribute.Expr.Range(),
					)
				}
				if !r.keyPattern.MatchString(k) {
					runner.EmitIssue(
						r,
						fmt.Sprintf(`tags key %q does not match valid pattern %s`, truncateLongMessage(k), `^[\p{L}\p{Z}\p{N}_.:/=+\-@]*$`),
						attribute.Expr.Range(),
					)
				}
				if len(v) > r.valueMax {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tags value for key %q must be 256 characters or less", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
			}
			return nil
		}, nil)
		if err != nil {
			return err
		}
	}

	return nil
}
