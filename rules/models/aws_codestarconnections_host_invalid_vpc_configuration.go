// This file generated by `generator/`. DO NOT EDIT

package models

import (
	"fmt"
	"regexp"

	"github.com/terraform-linters/tflint-plugin-sdk/hclext"
	"github.com/terraform-linters/tflint-plugin-sdk/logger"
	"github.com/terraform-linters/tflint-plugin-sdk/tflint"
)

// AwsCodestarconnectionsHostInvalidVpcConfigurationRule checks the pattern is valid
type AwsCodestarconnectionsHostInvalidVpcConfigurationRule struct {
	tflint.DefaultRule

	resourceType  string
	attributeName string
	keyMax        int
	keyMin        int
	keyPattern    *regexp.Regexp
	valueMax      int
	valueMin      int
	valuePattern  *regexp.Regexp
}

// NewAwsCodestarconnectionsHostInvalidVpcConfigurationRule returns new rule with default attributes
func NewAwsCodestarconnectionsHostInvalidVpcConfigurationRule() *AwsCodestarconnectionsHostInvalidVpcConfigurationRule {
	return &AwsCodestarconnectionsHostInvalidVpcConfigurationRule{
		resourceType:  "aws_codestarconnections_host",
		attributeName: "vpc_configuration",
		keyMax:        16384,
		keyMin:        1,
		keyPattern:    regexp.MustCompile(`^[\s\S]*$`),
		valueMax:      21,
		valueMin:      12,
		valuePattern:  regexp.MustCompile(`^vpc-\w{8}(\w{9})?$`),
	}
}

// Name returns the rule name
func (r *AwsCodestarconnectionsHostInvalidVpcConfigurationRule) Name() string {
	return "aws_codestarconnections_host_invalid_vpc_configuration"
}

// Enabled returns whether the rule is enabled by default
func (r *AwsCodestarconnectionsHostInvalidVpcConfigurationRule) Enabled() bool {
	return true
}

// Severity returns the rule severity
func (r *AwsCodestarconnectionsHostInvalidVpcConfigurationRule) Severity() tflint.Severity {
	return tflint.ERROR
}

// Link returns the rule reference link
func (r *AwsCodestarconnectionsHostInvalidVpcConfigurationRule) Link() string {
	return ""
}

// Check checks the pattern is valid
func (r *AwsCodestarconnectionsHostInvalidVpcConfigurationRule) Check(runner tflint.Runner) error {
	logger.Trace("Check `%s` rule", r.Name())

	resources, err := runner.GetResourceContent(r.resourceType, &hclext.BodySchema{
		Attributes: []hclext.AttributeSchema{
			{Name: r.attributeName},
		},
	}, nil)
	if err != nil {
		return err
	}

	for _, resource := range resources.Blocks {
		attribute, exists := resource.Body.Attributes[r.attributeName]
		if !exists {
			continue
		}

		err := runner.EvaluateExpr(attribute.Expr, func(val map[string]string) error {
			for k, v := range val {
				if len(k) > r.keyMax {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag key %q must be 16384 characters or less", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if len(k) < r.keyMin {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag key %q must be 1 characters or higher", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if !r.keyPattern.MatchString(k) {
					runner.EmitIssue(
						r,
						fmt.Sprintf(`tag key %q does not match valid pattern %s`, truncateLongMessage(k), `^[\s\S]*$`),
						attribute.Expr.Range(),
					)
				}
				if len(v) > r.valueMax {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag value for key %q must be 21 characters or less", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if len(v) < r.valueMin {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag value for key %q must be 12 characters or higher", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
				if !r.valuePattern.MatchString(v) {
					runner.EmitIssue(
						r,
						fmt.Sprintf(`tag value %q for key %q does not match valid pattern %s`, truncateLongMessage(v), truncateLongMessage(k), `^vpc-\w{8}(\w{9})?$`),
						attribute.Expr.Range(),
					)
				}
			}
			return nil
		}, nil)
		if err != nil {
			return err
		}
	}

	return nil
}
