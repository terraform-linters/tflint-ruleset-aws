// This file generated by `generator/`. DO NOT EDIT

package models

import (
	"fmt"
{{- if or (ne .KeyPattern "") (ne .ValuePattern "") }}
	"regexp"
{{- end }}

	"github.com/terraform-linters/tflint-plugin-sdk/hclext"
	"github.com/terraform-linters/tflint-plugin-sdk/logger"
	"github.com/terraform-linters/tflint-plugin-sdk/tflint"
)

// {{ .RuleNameCC }}Rule checks the pattern is valid
type {{ .RuleNameCC }}Rule struct {
	tflint.DefaultRule

	resourceType  string
	attributeName string
{{- if ne .KeyMax 0 }}
	keyMax        int
{{- end }}
{{- if ne .KeyMin 0 }}
	keyMin        int
{{- end }}
{{- if ne .KeyPattern "" }}
	keyPattern    *regexp.Regexp
{{- end }}
{{- if ne .ValueMax 0 }}
	valueMax      int
{{- end }}
{{- if ne .ValueMin 0 }}
	valueMin      int
{{- end }}
{{- if ne .ValuePattern "" }}
	valuePattern  *regexp.Regexp
{{- end }}
}

// New{{ .RuleNameCC }}Rule returns new rule with default attributes
func New{{ .RuleNameCC }}Rule() *{{ .RuleNameCC }}Rule {
	return &{{ .RuleNameCC }}Rule{
		resourceType:  "{{ .ResourceType }}",
		attributeName: "{{ .AttributeName }}",
{{- if ne .KeyMax 0 }}
		keyMax:        {{ .KeyMax }},
{{- end }}
{{- if ne .KeyMin 0 }}
		keyMin:        {{ .KeyMin }},
{{- end }}
{{- if ne .KeyPattern "" }}
		keyPattern:    regexp.MustCompile(`{{ .KeyPattern }}`),
{{- end }}
{{- if ne .ValueMax 0 }}
		valueMax:      {{ .ValueMax }},
{{- end }}
{{- if ne .ValueMin 0 }}
		valueMin:      {{ .ValueMin }},
{{- end }}
{{- if ne .ValuePattern "" }}
		valuePattern:  regexp.MustCompile(`{{ .ValuePattern }}`),
{{- end }}
	}
}

// Name returns the rule name
func (r *{{ .RuleNameCC }}Rule) Name() string {
	return "{{ .RuleName }}"
}

// Enabled returns whether the rule is enabled by default
func (r *{{ .RuleNameCC }}Rule) Enabled() bool {
	return true
}

// Severity returns the rule severity
func (r *{{ .RuleNameCC }}Rule) Severity() tflint.Severity {
	return tflint.ERROR
}

// Link returns the rule reference link
func (r *{{ .RuleNameCC }}Rule) Link() string {
	return ""
}

// Check checks the pattern is valid
func (r *{{ .RuleNameCC }}Rule) Check(runner tflint.Runner) error {
	logger.Trace("Check `%s` rule", r.Name())

	resources, err := runner.GetResourceContent(r.resourceType, &hclext.BodySchema{
		Attributes: []hclext.AttributeSchema{
			{Name: r.attributeName},
		},
	}, nil)
	if err != nil {
		return err
	}

	for _, resource := range resources.Blocks {
		attribute, exists := resource.Body.Attributes[r.attributeName]
		if !exists {
			continue
		}

		err := runner.EvaluateExpr(attribute.Expr, func(val map[string]string) error {
{{- $hasValueConstraints := or (ne .ValueMax 0) (ne .ValueMin 0) (ne .ValuePattern "") }}
			for k, {{ if $hasValueConstraints }}v{{ else }}_{{ end }} := range val {
{{- if ne .KeyMax 0 }}
				if len(k) > r.keyMax {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag key %q must be {{ .KeyMax }} characters or less", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
{{- end }}

{{- if ne .KeyMin 0 }}
				if len(k) < r.keyMin {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag key %q must be {{ .KeyMin }} characters or higher", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
{{- end }}

{{- if ne .KeyPattern "" }}
				if !r.keyPattern.MatchString(k) {
					runner.EmitIssue(
						r,
{{- if .Sensitive }}
						`tag key does not match valid pattern {{ .KeyPattern }}`,
{{- else }}
						fmt.Sprintf(`tag key %q does not match valid pattern %s`, truncateLongMessage(k), `{{ .KeyPattern }}`),
{{- end }}
						attribute.Expr.Range(),
					)
				}
{{- end }}

{{- if ne .ValueMax 0 }}
				if len(v) > r.valueMax {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag value for key %q must be {{ .ValueMax }} characters or less", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
{{- end }}

{{- if ne .ValueMin 0 }}
				if len(v) < r.valueMin {
					runner.EmitIssue(
						r,
						fmt.Sprintf("tag value for key %q must be {{ .ValueMin }} characters or higher", truncateLongMessage(k)),
						attribute.Expr.Range(),
					)
				}
{{- end }}

{{- if ne .ValuePattern "" }}
				if !r.valuePattern.MatchString(v) {
					runner.EmitIssue(
						r,
{{- if .Sensitive }}
						`tag value does not match valid pattern {{ .ValuePattern }}`,
{{- else }}
						fmt.Sprintf(`tag value %q for key %q does not match valid pattern %s`, truncateLongMessage(v), truncateLongMessage(k), `{{ .ValuePattern }}`),
{{- end }}
						attribute.Expr.Range(),
					)
				}
{{- end }}
			}
			return nil
		}, nil)
		if err != nil {
			return err
		}
	}

	return nil
}
